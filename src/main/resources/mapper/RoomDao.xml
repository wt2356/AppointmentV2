<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cdu.jaav.dao.RoomDao">

    <resultMap type="cdu.jaav.entity.Room" id="RoomMap">
        <result property="rId" column="r_id" jdbcType="INTEGER"/>
        <result property="location" column="location" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="allNum" column="all_num" jdbcType="INTEGER"/>
        <result property="num" column="num" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RoomMap">
        select
          r_id, location, name, status, all_num, num
        from library.room
        where r_id = #{rId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="RoomMap">
        select
          r_id, location, name, status, all_num, num
        from library.room
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="RoomMap">
        select
        r_id, location, name, status, all_num, num
        from library.room
        <where>
            <if test="rId != null">
                and r_id = #{rId}
            </if>
            <if test="location != null and location != ''">
                and location = #{location}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="allNum != null">
                and all_num = #{allNum}
            </if>
            <if test="num != null">
                and num = #{num}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="rId" useGeneratedKeys="true">
        insert into library.room(location, name, status, all_num, num)
        values (#{location}, #{name}, #{status}, #{allNum}, #{num})
    </insert>

    <insert id="insertBatch" keyProperty="rId" useGeneratedKeys="true">
        insert into library.room(location, name, status, all_num, num)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.location}, #{entity.name}, #{entity.status}, #{entity.allNum}, #{entity.num})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="rId" useGeneratedKeys="true">
        insert into library.room(location, name, status, all_num, num)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.location}, #{entity.name}, #{entity.status}, #{entity.allNum}, #{entity.num})
        </foreach>
        on duplicate key update
        location = values(location) , name = values(name) , status = values(status) , all_num = values(all_num) , num =
        values(num)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update library.room
        <set>
            <if test="location != null and location != ''">
                location = #{location},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="allNum != null">
                all_num = #{allNum},
            </if>
            <if test="num != null">
                num = #{num},
            </if>
        </set>
        where r_id = #{rId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from library.room where r_id = #{rId}
    </delete>

</mapper>